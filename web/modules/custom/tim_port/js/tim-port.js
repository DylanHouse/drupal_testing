(function($) {
  // Argument passed from InvokeCommand.
  $.fn.updateClientData = function(argument) {
    console.log('updateClientData is called.');
    // Set textfield's value to the passed arguments.
    //$('input#edit-output').attr('value', argument);
  };
})(jQuery);
